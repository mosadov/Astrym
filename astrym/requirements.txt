# ASTRYM 9.9.0 — Python 3.7+
# ============================================================
# Обязательные:
# ============================================================
dnspython>=2.0
requests>=2.25
rich>=13.0
networkx>=2.8           # Louvain + GEXF (community, gexf)

# ============================================================
# Опциональные:
# ============================================================
reportlab>=3.6          # PDF-экспорт (-f pdf)
phonenumbers>=8.12      # phone-команда
geoip2>=4.0             # локальная MaxMind GeoIP база
pycountry>=22.0         # точная нормализация стран в consensus

# ============================================================
# НЕ требуются (всё на stdlib):
# ============================================================
# - REST API (astrym_serve.py) — http.server из stdlib
# - STIX 2.1 — свой writer, без библиотеки stix2
# - GEXF — через networkx
# - Darkweb — через requests + Tor SOCKS